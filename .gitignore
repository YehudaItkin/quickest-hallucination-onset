# Data inputs and result outputs are never tracked.
# All input posteriors and all result files are JSON; none belong in git.
# (Inputs are derived from RAGTruth, which has its own license; outputs are
# regenerated by the scripts. See README for how to obtain/regenerate them.)
*.json

# Generated figures
figures/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/

# OS
.DS_Store
